#include <iostream>
#include <cassert>

// Venta* -> P -> Total
// Venta = N

int main () {

unsigned total{0};

for (unsigned venta; std::cin >> venta; ) 

total += venta; // total = total + x;

std::cout << total ;
}
